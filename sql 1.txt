NAME			:	SYED JAFAR SADIQ B.M
EMAIL ID		:	az.jaff880@gmail.com
QUALIFICATION		: 	B.E
SPECIALIZATION		:	COMPUTER SCIENCE ENGINEERING
UNIVERSITY		:	VISVESVARAYA TECHNOLOGICAL UNIVERSITY
DESIGNATION		:	SECURITY ENGINEER
COMPANY			:	CYBERWALL GLOBAL SOLUTIONS PVT. LTD.
COURSE NAME		:	AI ENGINEERING
BATCH ID		:	08-JUN-26-AEN-BUN-072-WDE2030-ONL
SUBJECT			:	SQL
TOPIC			:	DDL 	
INSTITUTION		:	DATAMITES GOBAL INSTITUE FOR DATA SCIENCE
LOCATION		:	BANGALORE
STATE			:	KARNATAKA
COUNTRY			:	INDIA
CONTACT NO.		:	9148713672 

---------------------------------------------------------------------------------------------------

				DATA DEFINATION LANGUAGE

DDL COMMANDS ARE :
1.	CREATE
2.	ALTER
3.	TRUNCATE
4.	RENAME
5.	DROP

1.	CREATE :

CREATE TABLE FOR EMPLOYEE DEATAILS AND PERFORM DDL OPERATIONS

SYNTAX  1 : CREATE DATABASE

CREATE DATABASE DATABASE_NAME;

SYNTAX 2 : INSIDE THE DATABASE CREATE TABLE

CREATE TABLE DATABASE_NAME.TABLE_NAME(
COLUMN_NAME 1 DATATYPE(SIZE),
COLUMN_NAME 2 DATATYPE(SIZE),
………………………………………………………………..
………………………………………………………………..
COLUMN_NAME N DATATYPE(SIZE)
);

CREATE EMPLOYEE TABLE 

CREATE TABLE JAFF_1.EMP(
EMP_ID VARCHAR(10),
EMP_NAME VARCHAR(30),
EMP-ADDRESS VARCHAR(50),
JOB VARCHAR(30),
MGR INT(4),
HIREDATE DATE,
SALARY INT(10,2),
COMMISION INT(10,2),
DEPT_NO INT(2)
);

DISPLAY THE STRUCTURE OF THE TABLE

DESC DATABSE_NAME.TABLE NAME;
DESC JAFF_1.EMP;
2.	ALTER :

PERFORM THE DDL OPERATIONS BY USING ALTER COMMAND

PROPERTIES OF ALTER COMMAND

•	ADD COLUMN

SYNTAX :
1a.	ADD NEW COLUMN TO THE TABLE

ALTER TABLE DATABASE_NAME.TABLE_NAME
ADD NEW_COLUMN_NAME DATA_TYPE(SIZE);

ALTER TABLE JAFF_1.EMP
ADD PAN_NO VARCHAR(10);

1b.	ADD MULTIPLE COLUMN TO THE EXISTING TABLE

ALTER TABLE JAFF_1.EMP
ADD COLUMN(SECURITY_STATUS VARCHAR(10),PINCODE INT(4));


•	DROP COLUMN

SYNTAX :

ALTER TABLE DATABASE_NAME.TABLE_NAME
DROP COLUMN COLUMN_NAME;

2a. DROP COLUMN FROM THE EXISTING TABLE

ALTER TABLE JAFF_1.EMP
DROP COLUMN PAN_NO;

2b. DROP MULTIPLE COLUMNS FROM THE EXISTING TABLE

ALTER TABLE JAFF_1.EMP
DROP COLUMN(SECURITY_STATUS,PINCODE);

•	RENAME :

SYNTAX :
ALTER TABLE DATABASE_NAME.TABLE_NAME
REANAME COLUMN OLD_COLUMN_NAME TO NEW_COLUMN_NAME;

3a. CHANGING THE NAME OF EXISTING COLUMN NAME

ALTER TABLE JAFF_1.EMP
RENAME COLUMN PINCODE TO PIN NO;

•	MODIFY :

SYNTAX :

ALTER TABLE DATABASE_NAME.TABLE_NAME
MODIFY COLUMN_NAME DATATYPE(SIZE);

4a. CHANGING THE DATA TYPE AND SIZE OF EXISTING COLUMN NAME

ALTER TABLE JAFF_1.EMP
MODIFY SECURITY_STATUS INT(20);

3.	TRUNCATE  :

SYNTAX :

TRUNCATE TABLE DATABASE_NAME.TABLE_NAME;

3a. TRUNCATE RECORD OF EMPLOYYE TABLE

TRUNCATE TABLE JAFF_1.EMP;

4.	RENAME :

SYNTAX :

RENAME TABLE DATABASE_NAME.OLD_TABLE_NAME
TO DATABASE_NAME.NEW_TABLE_NAME

4a. REANAME THE EXISTING TABLE

RENAME TABLE JAFF_1.EMP TO JAFF_1.EMP_1;


5.	DROP :

SYNTAX :

DROP  TABLE DATABASE_NAME.TABLE_NAME

5a. DROP THE EXISTING TABLE FROM THE DATABASE

DROP TABLE JAFF_1.EMP_1;



 
 



